var number = 19;
var newNum = [];
var nb = number.toString();
for (var i = 0; i < nb.length; i++) {
    var summ = (nb[i] * nb[i]) + (nb[i + 1] * nb[i + 1]);
    newNum.push(parseInt(nb[i]));
    if (summ >1) {
        
        }
    }
}